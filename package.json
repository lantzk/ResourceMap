{
    "name": "resource-conflict-zone-map",
    "version": "1.0.0",
    "description": "Application that shows potential future conflict zones over resources on a map",
    "main": "server/index.ts",
    "scripts": {
        "start": "bun run server/index.ts",
        "dev": "bun run --watch server/index.ts",
        "build": "echo 'Build script not needed with Bun'"
    },
    "dependencies": {
      "express": "latest",
      "sqlite3": "latest"
    },
    "devDependencies": {
      "@types/express": "latest",
      "@types/sqlite3": "latest",
      "nodemon": "latest",
      "ts-node": "latest",
      "typescript": "latest"
    }
  }